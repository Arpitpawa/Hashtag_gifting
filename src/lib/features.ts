/**
 * Feature switches. Flip to true when the client has finalised the feature.
 *
 * HAMPER_ENABLED — the "Build Your Hamper" builder (nav link, home section and
 * /build-hamper page). Off until boxes, cards, prices and photos are decided.
 */
export const HAMPER_ENABLED = false;
